rootProject.name = "bar-service-kotlin-client"

include("bar-service-models", "starter-service-client")

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}
